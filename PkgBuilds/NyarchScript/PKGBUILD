# SPDX-License-Identifier: GPL-3.0-or-later

pkgname=nyarchscript
pkgver=0.2.5.r4.34fdee9
pkgrel=1
pkgdesc="Run maintenance scripts easily on Nyarch Linux"
arch=('any')
url="https://github.com/NyarchLinux/NyarchScript"
license=('GPL-3.0-or-later')
depends=(
  'dconf'
  'gdk-pixbuf2'
  'glib2'
  'gtk4'
  'hicolor-icon-theme'
  'libadwaita'
  'pango'
  'python'
  'python-gobject'
)
makedepends=('git' 'meson')
source=("git+${url}.git")
sha256sums=('SKIP')

pkgver() {
  cd "${srcdir}/NyarchScript"
  _ver=$(git describe --long --tags 2>/dev/null | sed 's/\([^-]*-\)g/r\1/;s/-/./g')
  if [[ -n "${_ver}" ]]; then
    echo "${_ver}"
  else
    printf "0.0.0.r%s.g%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
  fi
}

build() {
  if [[ -d build ]]; then
    rm -rf build
  fi
  arch-meson NyarchScript build
  meson compile -C build
}

check() {
  meson test -C build --print-errorlogs || :
}

package() {
  meson install -C build --destdir "$pkgdir"
  chmod +x "${pkgdir}/usr/bin/${pkgname}"
  cd NyarchScript
  install -Dm644 README.md -t "${pkgdir}/usr/share/doc/${pkgname}"
  install -Dm644 COPYING -t "${pkgdir}/usr/share/licenses/${pkgname}"
}
